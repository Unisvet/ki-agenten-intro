[project]
name = "ki-agenten-intro"
version = "0.1.0"
description = "Interactive presentation on AI Agents for Digitaltag 2026"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
]

[tool.uv]
dev-dependencies = []
